import { Shell } from "@leight/mantine";
import { Callout } from "nextra-theme-docs";
import { SourceExample } from "../../../../components";
import { DefaultWeeks,DefaultWeeksExample } from "../../../../example/calendar-client";

# Weeks

This component alone is useful for paging through weeks of a month (common calendar as you know it).

`Weeks` is by default quite lightweight component as it does **not** allow select months/years as the full
[Calendar](./calendar) component.

## Minimal example

<Callout type={'info'} emoji={'ℹ️'}>
    This is the most minimal version of calendar you can get, but it is also basically feature-less.
</Callout>

<SourceExample
    example={<DefaultWeeksExample/>}
>
```tsx filename="DefaultWeeks"
import {
    Weeks,
    WeeksOfProvider
}                from "@leight/calendar-client";
import {type FC} from "react";

export interface IDefaultWeeksProps {
}

export const DefaultWeeks: FC<IDefaultWeeksProps> = () => {
    /**
     * Here you can use WeeksProvider or CalendarProvider.
     *
     * CalendarProvider provides all stuff needed for the whole Calendar (weeks/months/years/...).
     */
    return <WeeksOfProvider>
        <Weeks
            /**
             * Controls are enabled by default and do not create such overhead; this is just for
             * an example to render must minimal calendar.
             */
            withControls={false}
        />
    </WeeksOfProvider>
};
```
</SourceExample>
