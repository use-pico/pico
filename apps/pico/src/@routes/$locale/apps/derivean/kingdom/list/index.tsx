import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/$locale/apps/derivean/kingdom/list/")({
	component: () => <div>Hello "/$locale/apps/derivean/kingdom/list/"!</div>,
});
