# @use-pico/pico

Pico is successor to the original @leight-core packages; it's set of libraries based on `Mantine UI` and `Next.js`.

This is version 1.0, modern implementation of an old-school paradigm - it expects you're using different backend, than
Next.js.

For fullstack Next.js support, see version `2.0+`.
