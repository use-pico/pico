# 5.2 Token Definition

## Table of Contents
- [5.1 Contract Declaration](./5.1-contract-declaration.md)
- [5.2 Token Definition](./5.2-token-definition.md) *(current)*
- [5.3 Inheritance](./5.3-inheritance.md)
- [5.4 Runtime Overrides](./5.4-runtime-overrides.md)

---

Token definition is where you **assign concrete values** to your token declarations. This chapter teaches you how to create meaningful token values, build token hierarchies, and establish design system foundations.

## 🎯 What is Token Definition?

Token definition provides the **actual values** for your tokens. While declaration tells CLS *what* tokens exist, definition tells CLS *what values* those tokens have.

Think of it as **filling in the blanks** of your design vocabulary with actual colors, spacing, and other design values.

## 📝 Basic Token Definition

```tsx
import { cls } from "@use-pico/cls";

const ButtonCls = cls({
  token: {
    // Nested token structure with groups and variants
    "color.bg": {
      primary: ["bg-blue-600"],
      secondary: ["bg-gray-600"],
      danger: ["bg-red-600"]
    },
    "color.text": {
      primary: ["text-white"],
      secondary: ["text-gray-900"],
      danger: ["text-white"]
    },
    "spacing": {
      sm: ["px-3 py-1.5"],
      md: ["px-4 py-2"],
      lg: ["px-6 py-3"]
    }
  }
}, ({ what, def }) => ({
  token: {},
  rules: [
    def.root({
      root: what.css([
        "inline-flex items-center justify-center rounded font-medium"
      ])
    })
  ]
}));
```

## 🏗️ Token Value Patterns

### **Single Values**

Simple tokens with one CSS class:

```tsx
const SimpleTokens = cls({
  token: {
    "color.bg": {
      primary: ["bg-blue-600"]
    },
    "color.text": {
      primary: ["text-white"]
    },
    "spacing": {
      md: ["p-4"]
    },
    "border.radius": {
      md: ["rounded-md"]
    }
  }
});
```

### **Multiple Values**

Tokens that combine multiple CSS classes:

```tsx
const ComplexTokens = cls({
  token: {
    "color.bg": {
      primary: ["bg-blue-600", "hover:bg-blue-700"]
    },
    "color.text": {
      primary: ["text-white", "font-medium"]
    },
    "spacing": {
      md: ["px-4", "py-2"]
    },
    "border.radius": {
      md: ["rounded-md", "shadow-sm"]
    }
  }
});
```

### **Conditional Values**

Tokens that include responsive or state-based classes:

```tsx
const ResponsiveTokens = cls({
  token: {
    "spacing": {
      md: ["p-2", "md:p-4", "lg:p-6"]
    },
    "text.size": {
      md: ["text-sm", "md:text-base", "lg:text-lg"]
    },
    "border.radius": {
      md: ["rounded", "md:rounded-md", "lg:rounded-lg"]
    }
  }
});
```

## 🎨 Design System Token Patterns

### **Color System**

```tsx
const ColorTokens = cls({
  token: {
    // Background colors
    "color.bg": {
      primary: ["bg-blue-600"],
      secondary: ["bg-gray-600"],
      success: ["bg-green-600"],
      warning: ["bg-yellow-600"],
      danger: ["bg-red-600"],
      info: ["bg-blue-600"],
      surface: ["bg-white"],
      overlay: ["bg-black", "bg-opacity-50"]
    },
    
    // Text colors
    "color.text": {
      primary: ["text-blue-600"],
      secondary: ["text-gray-600"],
      success: ["text-green-600"],
      warning: ["text-yellow-600"],
      danger: ["text-red-600"],
      info: ["text-blue-600"],
      muted: ["text-gray-500"],
      inverse: ["text-white"]
    },
    
    // Border colors
    "color.border": {
      primary: ["border-blue-600"],
      secondary: ["border-gray-300"],
      focus: ["border-blue-500"],
      error: ["border-red-500"]
    }
  }
});
```

### **Spacing System**

```tsx
const SpacingTokens = cls({
  token: {
    // Padding
    "spacing.p": {
      xs: ["p-1"],
      sm: ["p-2"],
      md: ["p-4"],
      lg: ["p-6"],
      xl: ["p-8"],
      "2xl": ["p-12"]
    },
    
    // Padding X
    "spacing.px": {
      xs: ["px-1"],
      sm: ["px-2"],
      md: ["px-4"],
      lg: ["px-6"],
      xl: ["px-8"]
    },
    
    // Padding Y
    "spacing.py": {
      xs: ["py-1"],
      sm: ["py-2"],
      md: ["py-2"],
      lg: ["py-3"],
      xl: ["py-4"]
    },
    
    // Margin
    "spacing.m": {
      xs: ["m-1"],
      sm: ["m-2"],
      md: ["m-4"],
      lg: ["m-6"],
      xl: ["m-8"]
    }
  }
});
```

### **Typography System**

```tsx
const TypographyTokens = cls({
  token: {
    // Font sizes
    "typography.size": {
      xs: ["text-xs"],
      sm: ["text-sm"],
      base: ["text-base"],
      lg: ["text-lg"],
      xl: ["text-xl"],
      "2xl": ["text-2xl"],
      "3xl": ["text-3xl"]
    },
    
    // Font weights
    "typography.weight": {
      normal: ["font-normal"],
      medium: ["font-medium"],
      semibold: ["font-semibold"],
      bold: ["font-bold"]
    },
    
    // Line heights
    "typography.leading": {
      tight: ["leading-tight"],
      normal: ["leading-normal"],
      relaxed: ["leading-relaxed"]
    },
    
    // Text colors
    "typography.color": {
      primary: ["text-gray-900"],
      secondary: ["text-gray-600"],
      muted: ["text-gray-500"]
    }
  }
});
```

### **Layout System**

```tsx
const LayoutTokens = cls({
  token: {
    // Border radius
    "layout.radius": {
      none: ["rounded-none"],
      sm: ["rounded"],
      md: ["rounded-md"],
      lg: ["rounded-lg"],
      xl: ["rounded-xl"],
      full: ["rounded-full"]
    },
    
    // Shadows
    "layout.shadow": {
      none: ["shadow-none"],
      sm: ["shadow-sm"],
      md: ["shadow"],
      lg: ["shadow-lg"],
      xl: ["shadow-xl"],
      "2xl": ["shadow-2xl"]
    },
    
    // Borders
    "layout.border": {
      none: ["border-0"],
      thin: ["border"],
      thick: ["border-2"],
      thicker: ["border-4"]
    }
  }
});
```

## 🔧 Advanced Token Patterns

### **Semantic Token Mapping**

Map semantic concepts to design values:

```tsx
const SemanticTokens = cls({
  token: {
    // Success state
    "state.success": {
      bg: ["bg-green-600"],
      text: ["text-white"],
      border: ["border-green-600"],
      icon: ["text-green-500"]
    },
    
    // Error state
    "state.error": {
      bg: ["bg-red-600"],
      text: ["text-white"],
      border: ["border-red-600"],
      icon: ["text-red-500"]
    },
    
    // Warning state
    "state.warning": {
      bg: ["bg-yellow-600"],
      text: ["text-white"],
      border: ["border-yellow-600"],
      icon: ["text-yellow-500"]
    },
    
    // Info state
    "state.info": {
      bg: ["bg-blue-600"],
      text: ["text-white"],
      border: ["border-blue-600"],
      icon: ["text-blue-500"]
    }
  }
});
```

### **Component-Specific Tokens**

Define tokens specific to components:

```tsx
const ButtonTokens = cls({
  token: {
    // Button sizes
    "button.size": {
      sm: ["px-3", "py-1.5", "text-sm"],
      md: ["px-4", "py-2", "text-base"],
      lg: ["px-6", "py-3", "text-lg"]
    },
    
    // Button variants
    "button.variant": {
      primary: ["bg-blue-600", "text-white", "hover:bg-blue-700"],
      secondary: ["bg-gray-600", "text-white", "hover:bg-gray-700"],
      outline: ["border", "border-gray-300", "text-gray-700", "hover:bg-gray-50"],
      ghost: ["text-gray-700", "hover:bg-gray-100"]
    },
    
    // Button states
    "button.state": {
      disabled: ["opacity-50", "cursor-not-allowed"],
      loading: ["opacity-75", "cursor-wait"]
    }
  }
});
```

### **Theme-Aware Tokens**

Create tokens that adapt to different themes:

```tsx
const ThemeTokens = cls({
  token: {
    // Light theme
    "theme.light": {
      "color.bg": {
        surface: ["bg-white"],
        surfaceSecondary: ["bg-gray-50"]
      },
      "color.text": {
        primary: ["text-gray-900"],
        secondary: ["text-gray-600"]
      },
      "color.border": {
        default: ["border-gray-300"]
      }
    },
    
    // Dark theme
    "theme.dark": {
      "color.bg": {
        surface: ["bg-gray-900"],
        surfaceSecondary: ["bg-gray-800"]
      },
      "color.text": {
        primary: ["text-gray-100"],
        secondary: ["text-gray-400"]
      },
      "color.border": {
        default: ["border-gray-700"]
      }
    }
  }
});
```

## 🎯 Token Organization Strategies

### **By Design Category**

```tsx
const CategoryTokens = cls({
  token: {
    // Colors
    "color.bg": {
      primary: ["bg-blue-600"]
    },
    "color.text": {
      primary: ["text-blue-600"]
    },
    "color.border": {
      primary: ["border-blue-600"]
    },
    
    // Spacing
    "spacing.p": {
      sm: ["p-2"],
      md: ["p-4"],
      lg: ["p-6"]
    },
    
    // Typography
    "typography.size": {
      sm: ["text-sm"],
      md: ["text-base"],
      lg: ["text-lg"]
    },
    
    // Layout
    "layout.radius": {
      md: ["rounded-md"]
    },
    "layout.shadow": {
      md: ["shadow"]
    },
    "layout.border": {
      thin: ["border"]
    }
  }
});
```

### **By Component**

```tsx
const ComponentTokens = cls({
  token: {
    // Button tokens
    "button.color.bg": {
      primary: ["bg-blue-600"]
    },
    "button.color.text": {
      primary: ["text-white"]
    },
    "button.spacing": {
      md: ["px-4 py-2"]
    },
    "button.radius": {
      md: ["rounded-md"]
    },
    
    // Card tokens
    "card.color.bg": {
      surface: ["bg-white"]
    },
    "card.color.border": {
      default: ["border-gray-300"]
    },
    "card.spacing": {
      md: ["p-6"]
    },
    "card.radius": {
      md: ["rounded-lg"]
    },
    "card.shadow": {
      md: ["shadow"]
    },
    
    // Input tokens
    "input.color.bg": {
      surface: ["bg-white"]
    },
    "input.color.border": {
      default: ["border-gray-300"],
      focus: ["border-blue-500"]
    },
    "input.spacing": {
      md: ["px-3 py-2"]
    },
    "input.radius": {
      md: ["rounded"]
    }
  }
});
```

### **By Scale**

```tsx
const ScaleTokens = cls({
  token: {
    // Small scale tokens
    "scale.sm.color.bg": ["primary"],
    "scale.sm.spacing.p": ["default"],
    "scale.sm.typography.size": ["default"],
    "scale.sm.layout.radius": ["default"],
    
    // Medium scale tokens
    "scale.md.color.bg": ["primary"],
    "scale.md.spacing.p": ["default"],
    "scale.md.typography.size": ["default"],
    "scale.md.layout.radius": ["default"],
    
    // Large scale tokens
    "scale.lg.color.bg": ["primary"],
    "scale.lg.spacing.p": ["default"],
    "scale.lg.typography.size": ["default"],
    "scale.lg.layout.radius": ["default"]
  }
}, ({ what, def }) => ({
  token: {
    // Small scale definitions
    "scale.sm.color.bg": {
      primary: ["bg-blue-500"]
    },
    "scale.sm.spacing.p": {
      default: ["p-2"]
    },
    "scale.sm.typography.size": {
      default: ["text-sm"]
    },
    "scale.sm.layout.radius": {
      default: ["rounded"]
    },
    
    // Medium scale definitions
    "scale.md.color.bg": {
      primary: ["bg-blue-600"]
    },
    "scale.md.spacing.p": {
      default: ["p-4"]
    },
    "scale.md.typography.size": {
      default: ["text-base"]
    },
    "scale.md.layout.radius": {
      default: ["rounded-md"]
    },
    
    // Large scale definitions
    "scale.lg.color.bg": {
      primary: ["bg-blue-700"]
    },
    "scale.lg.spacing.p": {
      default: ["p-6"]
    },
    "scale.lg.typography.size": {
      default: ["text-lg"]
    },
    "scale.lg.layout.radius": {
      default: ["rounded-lg"]
    }
  }
}));
```

## 🔍 Best Practices

### **1. Consistent Naming**

```tsx
// ✅ Good: Consistent naming pattern
const GoodTokens = cls({
  token: {
    "color.bg": {
      primary: ["bg-blue-600"],
      secondary: ["bg-gray-600"]
    },
    "color.text": {
      primary: ["text-blue-600"],
      secondary: ["text-gray-600"]
    }
  }
});

// ❌ Avoid: Inconsistent naming
const BadTokens = cls({
  token: {
    "bgPrimary": {
      default: ["bg-blue-600"]
    },
    "color.bg": {
      secondary: ["bg-gray-600"]
    },
    "textColor": {
      default: ["text-blue-600"]
    },
    "color.text": {
      secondary: ["text-gray-600"]
    }
  }
});
```

### **2. Semantic Values**

```tsx
// ✅ Good: Semantic values
const SemanticTokens = cls({
  token: {
    "color.bg": {
      primary: ["bg-blue-600"],
      surface: ["bg-white"],
      overlay: ["bg-black", "bg-opacity-50"]
    }
  }
});

// ❌ Avoid: Non-semantic values
const NonSemanticTokens = cls({
  token: {
    "color.bg": {
      blue600: ["bg-blue-600"],
      white: ["bg-white"],
      black50: ["bg-black", "bg-opacity-50"]
    }
  }
});
```

### **3. Logical Grouping**

```tsx
// ✅ Good: Logical grouping
const LogicalTokens = cls({
  token: {
    // Color system
    "color.bg": {
      primary: ["bg-blue-600"],
      secondary: ["bg-gray-600"]
    },
    "color.text": {
      primary: ["text-blue-600"],
      secondary: ["text-gray-600"]
    },
    
    // Spacing system
    "spacing.p": {
      sm: ["p-2"],
      md: ["p-4"],
      lg: ["p-6"]
    },
    
    // Typography system
    "typography.size": {
      sm: ["text-sm"],
      md: ["text-base"],
      lg: ["text-lg"]
    }
  }
});
```

## 🚀 Common Patterns

### **Design System Foundation**

```tsx
const DesignSystemTokens = cls({
  token: {
    // Color palette
    "color.bg": {
      primary: ["bg-blue-600"],
      secondary: ["bg-gray-600"],
      success: ["bg-green-600"],
      warning: ["bg-yellow-600"],
      danger: ["bg-red-600"],
      info: ["bg-blue-600"],
      surface: ["bg-white"],
      overlay: ["bg-black", "bg-opacity-50"]
    },
    
    // Spacing scale
    "spacing.p": {
      xs: ["p-1"],
      sm: ["p-2"],
      md: ["p-4"],
      lg: ["p-6"],
      xl: ["p-8"],
      "2xl": ["p-12"]
    },
    
    // Typography scale
    "typography.size": {
      xs: ["text-xs"],
      sm: ["text-sm"],
      base: ["text-base"],
      lg: ["text-lg"],
      xl: ["text-xl"],
      "2xl": ["text-2xl"]
    }
  }
});
```

### **Component Library Tokens**

```tsx
const ComponentLibraryTokens = cls({
  token: {
    // Button tokens
    "button.size": {
      sm: ["px-3", "py-1.5", "text-sm"],
      md: ["px-4", "py-2", "text-base"],
      lg: ["px-6", "py-3", "text-lg"]
    },
    "button.variant": {
      primary: ["bg-blue-600", "text-white"],
      secondary: ["bg-gray-600", "text-white"],
      outline: ["border", "border-gray-300", "text-gray-700"]
    },
    
    // Card tokens
    "card.padding": {
      sm: ["p-4"],
      md: ["p-6"],
      lg: ["p-8"]
    },
    "card.radius": {
      sm: ["rounded"],
      md: ["rounded-md"],
      lg: ["rounded-lg"]
    },
    "card.shadow": {
      sm: ["shadow-sm"],
      md: ["shadow"],
      lg: ["shadow-lg"]
    }
  }
});
```

## 🎯 What's Next?

Now that you understand token definition, you're ready to explore:

- **[5.3 Inheritance](./5.3-inheritance.md)**: Learn how tokens inherit and compose across CLS instances
- **[5.4 Runtime Overrides](./5.4-runtime-overrides.md)**: Discover dynamic token overrides for theming

Token definition is the **art of creating meaningful design values**. Use these patterns to build consistent, maintainable design systems! 🎉

---

**Previous:** [5.1 Contract Declaration](./5.1-contract-declaration.md) | **Next:** [5.3 Inheritance](./5.3-inheritance.md)
